// Navy & Gold — the app's color theme (replaces the earlier Lake Turquoise
// palette; teal lives on as a secondary/accent color in a few closed
// decorative sets like Avatar.js's badge colors and textStyleColors.js's
// post-background options, intentionally not touched here). New
// screens/components should import these tokens rather than hardcoding hex
// values.
export const COLORS = {
  accent: "#D4A62A",
  accentInk: "#0B1F3A",
  wash: "#E9EEF7",
  bg: "#F5F7FB",
  surface: "#FFFFFF",
  ink: "#0B1F3A",
  sub: "#5C6F8A",
  border: "#DCE3EF",
};

export default COLORS;
